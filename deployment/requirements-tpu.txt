# Pinned TPU deployment environment (implementation spec section 5.2).
#
# This is the exact, internally compatible set verified on the target
# v5e-8 image (Python 3.12, torch/torch-xla 2.8.0 release line, libtpu
# 0.0.17, transformers 5.x). `torch` and `torch-xla` MUST stay on the same
# release line; the +cpu torch wheel is correct for a TPU VM (no CUDA).
#
# Install order matters: libtpu must be present before torch-xla imports,
# and torch-xla must see PJRT_DEVICE=TPU (see tpu_env.sh) at first import.

# --- torch / XLA core stack (PyTorch CPU wheels for TPU VMs) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0+cpu
torchvision==0.23.0+cpu
torchaudio==2.8.0+cpu
torch-xla==2.8.0
libtpu==0.0.17

# --- ComfyUI direct dependencies pinned to the verified image ---
numpy==2.5.0
transformers==5.12.1
tokenizers==0.23.0rc0
sentencepiece==0.2.1
safetensors==0.7.0
einops==0.8.2
aiohttp==3.14.1
yarl==1.24.2
pyyaml==6.0.3
Pillow==12.2.0
scipy==1.18.0
tqdm==4.68.3
psutil==7.2.2
requests==2.34.2
av==18.1.0
alembic
SQLAlchemy==2.0.52
filelock==3.29.4
simpleeval>=1.0.0
blake3
huggingface-hub
opencv-python-headless

# --- ComfyUI frontend / templates (pinned to the repo requirements.txt) ---
comfyui-frontend-package==1.49.6
comfyui-workflow-templates==0.11.41
comfyui-embedded-docs==0.5.10

# Not installed: torchsde (SDE samplers only; the er_sde profile does not use
# it) and comfy-kitchen / comfy-aimdo (optional accelerator packages; every
# import site degrades gracefully when absent).